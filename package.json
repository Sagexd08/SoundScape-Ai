{
  "name": "soundscape-ai-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "Frontend"
  ],
  "scripts": {
    "dev": "cd Frontend && npm run dev",
    "build": "cd Frontend && npm run build",
    "start": "cd Frontend && npm run start",
    "install-deps": "cd Frontend && npm install",
    "vercel-build": "cd Frontend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}
